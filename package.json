{
  "name": "openapi-ai-monorepo",
  "version": "1.0.0",
  "description": "OpenAPI AI SaaS Platform - Generates complete REST APIs from natural language description.",
  "private": true,
  "scripts": {
    "install-all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "dev-backend": "npm run dev --prefix backend",
    "dev-frontend": "npm run dev --prefix frontend",
    "dev": "concurrently \"npm run dev-backend\" \"npm run dev-frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "groq-sdk": "^1.3.0"
  }
}
